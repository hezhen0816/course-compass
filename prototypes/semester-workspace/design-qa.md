# 第一階段原型驗證

日期：2026-09-09。以使用者確認的「本學期課表＋找課側欄」方向驗證；這是重新設計，不宣稱與舊介面逐像素相同。

## 已驗證

- 獨立 Vite production build 通過；未變更正式 Web 入口與相依套件。
- 3 項 Node 測試通過：多時段重疊／相鄰節次／不同星期、範例課程衝堂、儲存內容損壞及去重。
- Chrome 桌面實際畫面：週課表、找課列表、課程詳情、預排虛線與衝堂提示。
- 加入資料庫系統後，預排 3 → 6 學分，已選保持 18 學分；重整仍保留。
- 移除資料庫系統後，預排回到 3 學分。
- 人工智慧概論顯示與資料結構衝堂；加入按鈕停用。
- 查詢不存在課程顯示空結果，清除條件可恢復列表。
- 不衝堂篩選顯示 4 門，再選本系選修縮小為資料庫系統 1 門。
- 390×844 手機 viewport：預設清單；搜尋資料庫、開啟詳情、加入預排與關閉面板均成功，回到清單顯示預排 6 學分。
- Chrome console 未回報 warning／error（檢查時點）；沒有正式服務請求。

## 可讀性修正

- 補上手機版週課表／清單切換按鈕的可存取名稱。
- 加大課名及次要文字；衝堂外框只標示真正重疊的時段。
- 手機面板鎖定背景捲動，提供 Escape 關閉及 Tab 循環。

## 範圍與限制

- 桌面及手機清單畫面已視覺檢視。手機後續詳情截圖遇到瀏覽器擷取逾時，但操作與狀態已由可存取樹確認。
- 尚未完成實機觸控、螢幕閱讀器及完整鍵盤流程測試；不宣稱 WCAG 合規。
- 學校階段規則、正式加退選、真實學分認列與監控不屬於本階段。

final result: passed (first-stage prototype scope; limitations above)

## 滾輪回報後的修正（2026-09-09）

- 使用者回報 Chrome 原型滾輪無反應。移除桌面課表不需要的 `overflow-x: auto`，圓角裁切改用 `overflow: clip`，避免建立多層捲動容器；窄螢幕仍保留課表水平捲動。
- 獨立內建瀏覽器從課表區域捲動，頁面 scrollY 由 0 → 100 → 654.5，可見第 10 節及頁尾。
- Chrome 測試期間曾有原生捲動成功，但後續測試受分頁切換／控制中斷影響，尚未確認使用者的 Chrome 滾輪問題已消失；不把獨立預覽通過視為 Chrome 修復完成。


## 初選重疊志願（2026-09-09）

- 5 項 Node 測試通過，新增初選允許／加退選阻擋、不可重複加入、三門同時段分欄、相鄰時段與不同星期、預覽不重複等情境。
- Chrome：初選加入「行銷管理」後與「成本會計」週二 2–4 節並排顯示，兩門均可點擊；志願總量更新為 9 學分，已選仍為 18。
- 切換加退選，課程保留並顯示 2 門重疊草稿；移除行銷管理後，重新加入按鈕因衝堂停用。
- 切回初選可再次加入，重整後仍保留初選模式及兩門重疊志願。Chrome console 無 warning/error。
- 新增模擬行銷管理後搜尋樣本由 5 門增為 6 門；前段原始篩選數量為第一版驗證時點。
- 此次未修改正式選課行為，未實作官方志願排序或抽選。模式為手動情境預覽。
- 390×844 內建瀏覽器：初選／加退選按鈕均可操作，初選說明與志願總量可閱讀；本輪未再測完整手機加課流程。
